services:
  - type: web
    name: tvgids
    runtime: node
    plan: free
    buildCommand: npm ci && npm run import && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "24"
      - key: SITE_URL
        sync: false
